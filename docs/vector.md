# Vector

V37.8 focuses on physical form and direct control. The carrier's major surfaces gain stronger depth and material separation; restrained cyan energy and champagne metal identify its functional areas. Scene graphics remain local and bounded. No new runtime dependency or texture download is required.

## Engineering controls

Propulsion and hull inspection are continuous controls from 0 to 100. Defaults are 50 and 0. The propulsion field changes appearance without affecting requests, permission, camera or real infrastructure. Hull inspection moves the actual section plane, while the existing open/close action retains its endpoints. Paused and reduced-motion visitors receive immediate static updates. Mobile layout reduces the distance between controls and the scene; secondary details remain available through native disclosures.

## The living opening

Awaken the scene mounts the existing locally served Lightwake film only after explicit activation. The eight-second film is silent and never loops. The original poster and still headline remain in place; a decoded film frame appears above the poster and below the reading shade. Registered Bit annotations disappear while the camera moves, then return with the original still. Full cinema links remain available.

Pause, resume, replay, restore-still and failure recovery are explicit. Global pause, open dialogs, hidden pages and an offscreen hero cancel playback intent. Returning to the page does not silently resume a film. Reduced motion starts with the still; visitors may explicitly play the finite film. Native playback is used without duplicating the cinema's scrubbing/fallback logic.

## Release contract

Version `37.8.0`, title `Cashio V37.8 — Vector | Doug Cashio`. Initial stylesheet limits remain 195,000 raw and 42,000 gzip bytes. Existing evidence dates, animated identity, five cinema films, observatory PNG export, studies and archived routes remain intact. Standing publication approval covers the guarded PR and Pages release. Baseline and rollback: `f4d317c5bc5e0a26ced25bc85746f451137435a8`.
