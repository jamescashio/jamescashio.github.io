# Parallax

V37.7 lets visitors change their perspective on the Cashio universe. Gold, cyan, graphite, and warm stone remain the material language. Interaction changes the scene or explains a decision; reading text stays still.

## World workshop

The lazy observatory exposes cloud cover, aurora strength, and sun direction. Defaults reproduce the prior scene. Manual changes render even while paused or under reduced motion, and stop the finite camera journey. The first aurora adjustment ignites the gate with visible state feedback. Save this view captures a fresh canvas frame as a PNG without enabling persistent drawing buffers or adding a GPU pass. Export errors are announced and can be retried; temporary URLs are released. The workshop keeps the scene nearby on narrow screens.

Rendering remains capped at two million pixels and 30 fps, with 39 draw calls and 44,098 triangles at the most detailed active view. Workshop styles load only with the observatory. Initial styles retain the 195,000-byte raw and 42,000-byte gzip limits.

## Request journey and navigation

The atlas illustrates four stages: a human defines intent, HERMES coordinates, the selected Zeus or Apollo compute handles work, and a human reviews the result. It is a conceptual explanation, not live telemetry. Manual steps remain available with reduced motion. Playback ends, can be replayed, and suspends while paused, hidden, offscreen, or behind an experience dialog. Selecting an atlas node stops the trace and retains the original detail behavior.

Study navigation consolidates repeated labels and controls on phones. Seven illustrated tabs, keyboard navigation, selected models, and shared hashes remain intact. The header surface is fully opaque so the signature's screen blend stays consistent over cream and dark sections.

## Within the light

This section documents the original Parallax film. The current `/#film=sanctuary` experience is the longer [The inner light](sanctuary.md); the original media files below remain archived.

An original film begins from the existing sanctuary artwork. A deliberate forward camera move and slight arc reveal the twin fluted monoliths and turning amber core. Cyan circuits answer along the machined platform. The film contains no live infrastructure footage.

- Video: `public/assets/parallax/sanctuary-awakens.mp4`, 1920 × 1080, 24 fps, 8 seconds, silent H.264, 3,836,845 bytes.
- Poster: `public/assets/parallax/sanctuary-awakens-poster.webp`, 1280 × 720 WebP, under 100 KB.
- Optional local delivery, metadata before media, half-second keyframes, no initial media preload.
- Explicit Play, Pause, Replay, timeline, and Threshold, Approach, Within frame landmarks.
- `/#film=sanctuary` opens a still poster. Explore the working studies closes the cinema, updates the destination, and focuses the study heading.

Five horizontally scrollable choices preserve every earlier film and link. Seeking remains native on byte-range hosts, with the existing bounded in-memory fallback for progressive hosts. The public content policy is unchanged. Switching, hiding, or closing the film stops playback and releases its fallback copy.

## Release

Software version: `37.7.0`. Page title: `Cashio V37.7 — Parallax | Doug Cashio`. Receipt identity retains `THE HUMAN RECKONING`, `Lensing`, and `Lensing Observatory`. Fleet and routing dates remain unchanged. The baseline and rollback target is `ef7a8fe3bbb11f79abfb12e9f031d7ed49674eb6`. Standing owner approval authorizes the protected-main PR, Pages deployment, and subsequent verification of the actual public experience.
