# V36 EVENT HORIZON — Aurora approval candidate

The user explicitly authorized a complete redesign, original artwork, stronger sci-fi colors, and substantially more ambitious interactions. All work stays local for review. Publishing is a separate decision.

## Creative direction

An editorial journey through Doug Cashio’s universe: an engineered orbital ring, warm stone and graphite, champagne light, readable space, and a human at the center. Exo provides engineered clarity; a contrasting editorial serif gives the headlines warmth. Bit becomes the faceted orbital core. Dune, space exploration, and flight-test discipline inform the mood without copying franchise assets.

The page alternates cinematic space, a light technical atlas, an imagined computing sanctuary, the working project laboratory, a dated evidence archive, real flight heritage, and the accountable operator. A single composition dominates each section. Original hero and sanctuary images are generated concept art and visibly identified as such.

## Useful interactions

- A fold transition moves from the orbital opening to the system atlas. It lasts 1.65 seconds and skips immediately under reduced motion. Artwork, Bit, the tracking guide and the fold share the same responsive origin.
- Four native atlas controls explain operator authority, HERMES, Zeus, and Apollo. Relationships are conceptual; service locations are not invented.
- Seven studies explain the existing seven builds. Routing, escalation, exposure triage, evidence composition, freshness, signal qualification, and transitive dependency impact have working deterministic controls.
- E.V.E. reads the existing public archive locally. It has command history, clear, named examples, and usable responses without a network dependency.
- The four aircraft retain the original credited source imagery and supporting editorial content. Working principles are distinguished from historical quotations.
- A local preview link preserves each selected study. Email, professional profiles, public evidence, and the original command deck remain reachable.

## Evidence boundary

Fleet evidence is dated 28 August 2026; routing inventory is dated 21 August 2026. Counts are observations, not current telemetry. Every new simulation is explicitly illustrative. The ZeusApollo refresh prompt is supplied separately. No facts are silently upgraded to current status, and no private service mapping or credentials are added.

## Accessibility and performance

All substantive copy renders at build time. Self-hosted fonts, responsive AVIF/WebP images, reserved image dimensions, one small visibility-aware star canvas, and no new runtime libraries keep the experience lean. Audio defaults off and responds only to deliberate activation. Motion follows reactive system preferences and has a manual pause control. Native links, buttons, tabs, labels, dialogs, focus treatment, and keyboard operation support the complete journey. Secondary animation pauses when its section leaves the viewport.

Verify actual desktop, tablet, 390 px and 320 px layouts; all seven studies; invalid, direct, and history navigation; modal focus; keyboard use; quiet motion; network failure; console errors; and the built artifact. Automated scores are measured results, never a promise of aesthetic perfection.

## Integration

The owner approved publication as V36 — THE HUMAN RECKONING. The completed experience is promoted to `/`; `/odyssey.html` remains an alias and the V35 command deck is preserved at `/command-deck.html`. Existing `/#deck=…` links redirect with their query and fragment intact. Software release metadata is separate from the unchanged August evidence snapshots. Publication uses the existing GitHub Pages workflow and does not alter DNS, Cloudflare policy, or live infrastructure. The original release name is inspired by the Butlerian Jihad in Frank Herbert’s _Dune_.

## Final Aurora refinement — 36.0.0-preview.3

### Sovereign Worlds

The owner additionally requested a 3D world that compares Sovereign AI. A new visitor-operated world compares fully local, hybrid and cloud-only deployment patterns against exactly twelve illustrative requests. Mixed sensitivity means six public and six sensitive requests. The same workload is used in every architecture.

Local mode processes everything within the owned boundary. Hybrid keeps sensitive requests local and sends public requests to the cloud while connected, falling back to local processing when disconnected. Cloud-only holds requests when disconnected and holds sensitive requests until the visitor explicitly permits egress. The model assumes both local and cloud systems support the workload and sufficient local capacity exists for hybrid fallback. It is a routing illustration, not a provider ranking, legal assessment, cost claim, or AI quality/performance benchmark.

Controls and the complete text result are available independently of graphics. The real 3D renderer and its existing Three.js dependency load only after an explicit launch. Manual camera controls remain usable with reduced motion; playback is opt-in, bounded, and paused offscreen or in a hidden document. The world sends no requests to actual AI or infrastructure services. Mission Control, the primary navigation, and the hero's secondary invitation lead directly to it.

### Finishing details

The direction is now deep navy, blue titanium, ice cyan, solar gold, and restrained coral, alternating with cool paper sections. Original orbital artwork and the Observatory share illuminated cyan rim details and a faceted gold Bit. Richness comes from composition, precise edges, reflections, and legible hierarchy.

The final candidate adds an immersive, visitor-operated Observatory cinema view. One instrument and one canvas retain the selected mission, orientation and orbit state when entering or leaving the native modal dialog. Escape and a visible close control return the visitor to the opener. Every mission, orientation and export control remains available; no motion or audio begins merely by opening cinema.

The system atlas becomes an engraved architectural instrument with bespoke node graphics, radial scales and a brief selection trace. The operator receives an original SVG House Cashio insignia. Neither invents infrastructure facts or adds an external asset dependency.

Mobile study tabs announce their horizontal orientation, support Left/Right arrows, and keep a selected deep-linked study in view without scrolling the reader's page. Contact typography stays balanced at 320 px. Footer controls are more legible, the original experience is explicitly labeled V35, and email copying announces its actual result.

Acceptance is a complete local candidate: production build, source and artifact regressions, privacy and release consistency checks, desktop/390/320 browser coverage, working cinema focus and state preservation, all seven studies, reactive reduced motion, clean console, and fresh local performance measurements. Automated measurements are evidence, not a claim of a perfect aesthetic score. Doug reviews this candidate before any public release is considered.

## Sovereign Starship — 36.0.0-preview.4

The review candidate now leads with an original exploration spacecraft. Champagne and ivory armor, deep graphite recesses, a gold Bit command core, and cyan propulsion give the ship a distinct silhouette. The ship is owned compute and the separate orbital relay is an external provider. Camera presets, physical system inspection, and a hull cutaway explain the mapping. A real-render poster retains the same identity before launch; the renderer remains lazy. Routine flight, Deep-space blackout, and Classified mission are shortcuts into the existing twelve-request model. Controls, full text results, assumptions and explicit private-data permission remain authoritative.

The seven project studies receive coordinated engineered illustrations, keeping their original controls and examples. Motion describes a selected route, changing instrument, or bounded decision; it does not imply fresh operational measurements. Global pause, reduced motion and offscreen lifecycle remain part of acceptance.

Display typography uses the locally served [Oxanium font from Google Fonts](https://github.com/google/fonts/tree/main/ofl/oxanium), with its [SIL Open Font License](../public/fonts/Oxanium-OFL.txt) retained. Existing Exo body type and Jet instrument labels preserve readability. No font service is contacted by the page.

The logo is the owner's supplied Cashio AI artwork. The GIF adds a six-second sweep to its existing luminous blue detail, retaining lettering and proportions. Screen blending removes the dark matte on the page, without a frame or background tile. A brighter, smoothly clipped 4.8-second light pass makes the motion visible at navigation size. Both effects stop for reduced motion, paused motion, hidden pages and offscreen marks; a still PNG is used instead. The animated GIF has explicit dimensions and loads after the initial page settles.

The operator card replaces the static DC monogram with the same Cashio mark inside an original orbital identity panel. Gold and cyan arcs turn in opposite directions, a cyan signal follows the outer track, and a small gold Bit floats below the mark. The wordmark remains stationary and legible. Motion runs only while the card is visible, the page is active, and the visitor permits motion; reduced motion retains the complete still composition. This is decorative identity artwork, not an authentication badge or live status display.
